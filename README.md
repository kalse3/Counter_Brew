# Counter Brew

Café drinks you can make on your kitchen counter — no espresso machine required.

## Author
Elissa Kastoun / Kals

## Live site
[Add the deployment URL once deployed]

## API used
Spoonacular API — used on the Explore page to search drink recipes live, with search, filtering, and pagination over the fetched results.

## Project description
[2–3 sentences in your own words: the two halves — my 15 curated home recipes, and the live API explorer.]

## Custom front-end requirement
Sticky footer with social links and contact info, shown on every page.
[After building it: 1–2 sentences on how I implemented it.]

## Curated content
15+ café drinks I selected and wrote up myself, each with attributes (base, milk, sweetness, method) and step-by-step home instructions.

## Running locally
Uses ES6 modules, so it must be served over http, not opened as a file. In VS Code: install the "Live Server" extension, right-click index.html → "Open with Live Server".

## AI-use appendix
Tools used:
- [tool — what I used it for]

Sample prompts:
1. [a real prompt I used]
2. [a real prompt I used]

What the AI got wrong and how I fixed it:
1. [a specific thing that was wrong/didn't work + how I found and fixed it]
2. [another]

## Known limitations
- The API key runs client-side, so it's visible in the browser on the deployed site (an accepted constraint for a static, backend-less project). It's kept out of this repo via .gitignore.